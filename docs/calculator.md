# 简单计算器语言语法规则

## 1. 语言特性
- **算术操作**：支持加法、减法、乘法、除法。
- **逻辑操作**：支持与、或、异或。
- **移位操作**：支持算术和逻辑移位。

---

## 2. 示例程序
以下是一个完整的示例程序，用于计算 (A + B) * C 并将结果存储到内存地址 0x10：
```plaintext
A = 5
B = 10
result = A + B
```